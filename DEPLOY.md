# Deploy paths (Render / Vercel)

| Service | Root Directory |
|---------|----------------|
| **Render (backend)** | `microservices/microservices/backend` |
| **Vercel (frontend)** | `microservices (2) (1)/microservices/microservices/frontend` *(submodule — move pending)* or import frontend repo separately |

See `microservices/microservices/docs/deploy-free.md` for full steps.
